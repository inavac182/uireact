import { Metadata } from '@uireact/docs-tools';

import packageJson from './package-metadata.json';

# @UiReact/create-next-app

<Metadata packageName='create-next-app' packageJson={packageJson} />

> Script used to quickly get a NextJS Project set up with @UiReact up and running.

## Usage

1. Run script

```
npx @uireact/create-next-app
```

2. Start it up

```
npm run dev
```
