import { UiLineSeparator } from '@uireact/separator';
import packageJson from './package-metadata.json';
import { Metadata } from '@uireact/docs-tools';

# UiSeparators

<Metadata packageName='separator' packageJson={packageJson} description='A separator element to split different contents in a section' includeInformation />

## UiLineSeparator

```jsx live scope={{UiLineSeparator}}
  <>
    <UiLineSeparator />
    <br />
    <UiLineSeparator inverse />
  </>
```
