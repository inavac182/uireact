---
name: UiTable
menu: Components
---

import { Props } from 'docz';

import * as packageJson from '../package.json';
import Playground from '../../../src/Playground';

import { UiTable } from '../src';
import { UiBadge } from '@uireact/badge';

<UiBadge category="warning">❗️ Beta</UiBadge>

# UiTable

<sup>
  <a href="https://github.com/inavac182/ui-react/blob/main/packages/COMPONENT-NAME/" target="_blank">
    v{packageJson.version}
  </a>
</sup>

> Used to render information in tables that are filterable and sortable

## Installation

> npm i @uireact/foundation @uireact/table

## UiTable

<Playground>
  <UiTable
    data={{
      headings: ['id', 'Summary', 'Price'],
      fields: [
        ['1', 'item 1', '$10'],
        ['3', 'item 2', '$20'],
        ['3', 'item 3', '$30'],
      ],
    }}
  />
</Playground>

## UiTable - with category

<Playground>
  <UiTable
    data={{
      headings: ['id', 'Summary', 'Price'],
      fields: [['1', 'item 1', '$10']],
    }}
    category="tertiary"
  />
</Playground>

### Props

<Props of={UiTable} />
