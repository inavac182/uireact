---
name: UiView
menu: Components
---

import * as packageJson from '../package.json';
import Playground from '../../../src/Playground';
import { UiView } from '../src';
import { ViewExample } from './util';

# UiView

<sup>
  <a href="https://github.com/inavac182/ui-react/blob/main/packages/view/" target="_blank">
    v{packageJson.version}
  </a>
</sup>

> UiView component that wraps the content of your react application already creating the theme context

## Installation

> npm i @uireact/foundation @uireact/view

## Just a view

The view component already instanciates the ThemeContext so it only needs the theme and the selected color as props

<Playground hideThemeSelector>
  <ViewExample />
</Playground>

## View with centered content

<Playground hideThemeSelector>
  <ViewExample centeredContent />
</Playground>

### Props

<Props of={UiView} />
