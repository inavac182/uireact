import { ThemeTokens } from './utils';

import { UiLineSeparator } from '@uireact/separator';
import packageJson from './package-metadata.json';
import { Metadata } from '@uireact/docs-tools';

import { UiCard } from '@uireact/card';

# Default theme
<Metadata packageName='foundation' packageJson={packageJson} />

<UiCard styling='outlined' category='warning'>
    <h2>Work in progress</h2>
    <br/>
    <p>The default theme is being re-worked given that we are moving away from styled-components to SCSS modules.</p>
    <br/>
    <p>On the V3 version the theme will br provided as a set of CSS variables where all components will consume their values.</p>
</UiCard>
