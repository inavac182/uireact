---
name: UiButton
menu: Components
---

import { Props } from 'docz';

import Playground from '../../../src/Playground';
import ThemePresentation from '../../../src/ThemePresentation';
import { UiPrimaryButton, UiSecondaryButton, UiTertiaryButton } from '../src/';
import { PrimaryButtonThemeMapper, SecondaryButtonThemeMapper, TertiaryButtonThemeMapper } from '../src/theme/';
import { UiSpacing, Sizing } from '../../foundation/src/';
import * as packageJson from '../package.json';

# UiButton

<sup>
  <a href="https://github.com/inavac182/ui-react/blob/main/packages/button/" target="_blank">
    v{packageJson.version}
  </a>
</sup>

> More than a button

## Installation

> npm i @uireact/foundation @uireact/button

## UiPrimaryButton

<Playground>
  <UiPrimaryButton>
    <UiSpacing margin={{ all: Sizing.three }}>Button! 💅🏾</UiSpacing>
  </UiPrimaryButton>
</Playground>

#### Props

<Props of={UiPrimaryButton} />

#### Theme

<ThemePresentation themeMapper={PrimaryButtonThemeMapper} />

## UiSecondaryButton

<Playground>
  <UiSecondaryButton>
    <UiSpacing margin={{ all: Sizing.three }}>Button! 💅🏾</UiSpacing>
  </UiSecondaryButton>
</Playground>

#### Props

<Props of={UiSecondaryButton} />

#### Theme

<ThemePresentation themeMapper={SecondaryButtonThemeMapper} />

## UiTertiaryButton

<Playground>
  <UiTertiaryButton>
    <UiSpacing margin={{ all: Sizing.three }}>Button! 💅🏾</UiSpacing>
  </UiTertiaryButton>
</Playground>

#### Props

<Props of={UiTertiaryButton} />

#### Theme

<ThemePresentation themeMapper={TertiaryButtonThemeMapper} />
