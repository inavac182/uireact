{
  "name": "@uireact/navbar",
  "description": "Navbar component from uireact library",
  "repository": "https://github.com/inavac182/ui-react",
  "license": "MIT",
  "version": "0.4.11",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": " rm -rf dist node_modules coverage",
    "eslint": "eslint ./src/**/*.{ts,tsx} ./__tests__/**/*.tsx --max-warnings=0",
    "lint:styles": "stylelint ./src/**/*.tsx",
    "lint": "npm run lint:styles && npm run tsc && npm run eslint",
    "test": "jest",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch"
  },
  "devDependencies": {
    "@uireact/foundation": "^0.16.0"
  },
  "peerDependencies": {
    "@types/styled-components": "^5.1.0",
    "@uireact/foundation": "^0.7.2",
    "react": ">= 17 < 19",
    "react-dom": ">= 17 < 19",
    "styled-components": "^5.1.1",
    "tslib": "^2.5.0"
  }
}
